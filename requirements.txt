# ── Coeur : donnees et modelisation ──────────────────────────────────────────
numpy>=1.26,<3
pandas>=2.2
scikit-learn>=1.4
scipy>=1.11
joblib>=1.4
openpyxl>=3.1          # lecture des exports .xlsx
xlrd>=2.0              # lecture des fiches .xls fournies par OCP
PyYAML>=6.0            # referentiel domaine (tags.yaml, amdec.yaml)

# ── API et dashboard ─────────────────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.7
httpx>=0.27

# ── Journalisation et configuration ──────────────────────────────────────────
loguru>=0.7
python-dotenv>=1.0

# ── Tests ────────────────────────────────────────────────────────────────────
pytest>=8.0
pytest-cov>=5.0

# ── Analyse (notebooks) ──────────────────────────────────────────────────────
matplotlib>=3.8
jupyter>=1.0
pypdf>=5.0             # extraction textuelle des gammes PDF lors de l'audit

# ── Optionnel : redaction par modele de langage ──────────────────────────────
# Le systeme fonctionne integralement sans ces paquets ni cle API. Les installer
# et renseigner GEMINI_API_KEY dans .env active la couche de redaction.
# langchain-core>=0.3
# langchain-google-genai>=2.0

# ── Optionnel : explicabilite SHAP ───────────────────────────────────────────
# L'attribution par occlusion exacte est utilisee par defaut : exacte,
# deterministe et sans dependance lourde. SHAP est utilise s'il est present.
# shap>=0.46

# ── Qualite de code (developpement) ──────────────────────────────────────────
ruff>=0.6
bandit>=1.7
# mypy : cible `make types`, informative. La configuration existait dans
# pyproject.toml sans que l'outil soit installe nulle part.
mypy>=1.11

# ── Scripts auxiliaires ──────────────────────────────────────────────────────
# Non necessaires au poste ni aux tests, mais importes par deux scripts du
# depot. Un fichier de dependances qui ne couvre pas son propre depot laisse un
# ModuleNotFoundError a qui installe le projet et lance l'un d'eux.
Pillow>=10.0          # scripts/make_contact_sheets.py — planches de contact
python-docx>=1.1      # scripts/update_report_docx.py — export du rapport
