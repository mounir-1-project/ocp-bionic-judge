# Database
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
alembic==1.13.1

# Core ML
numpy==1.26.4
pandas==2.2.2
# scikit-learn 1.7.x: (1) hdbscan depends on scikit-learn>=1.6, (2) scikit-learn 1.4.x
# triggers a "metaclass conflict" on Python 3.12, and (3) we cap below 1.8 because
# newer releases require numpy>=2 while this project pins numpy 1.26.4 — mixing them
# breaks sklearn's base classes (the same metaclass error). 1.7.x works with numpy 1.26.
scikit-learn>=1.6,<1.8
scipy>=1.13,<2
joblib>=1.4

# Explainability
shap>=0.46

# Clustering / Anomaly Detection
hdbscan>=0.8.40

# MLflow
mlflow>=2.14.3

# Protobuf — pin required: mlflow>=2.14.3 + langchain-google-genai both need proto5
protobuf>=5.29.6,<6.0

# LangChain + Google Gemini (gratuit via Google AI Studio)
langchain==0.3.3
langchain-google-genai>=2.0.0,<3.0.0
langchain-community==0.3.2

# API
fastapi==0.111.0
uvicorn[standard]==0.30.1
httpx==0.27.0
PyJWT>=2.8.0
slowapi==0.1.9
python-multipart==0.0.9

# Dashboard Streamlit (deprecie — remplace par le frontend React/Vite)
# streamlit==1.36.0
# plotly==5.22.0
# streamlit-autorefresh==1.0.1

# Data Validation — pydantic>=2.7.4 required by langchain 0.3.3
pydantic>=2.7.4
pydantic-settings>=2.3.4

# Environment
python-dotenv==1.0.1

# Logging
loguru==0.7.2

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0

# Utilities
tqdm==4.66.4
aiofiles==23.2.1
